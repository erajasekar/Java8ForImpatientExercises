package chapter2;

import java.util.stream.Stream;

/**
 * Problem : Write a method {@code public static <T> Stream<T> zip(Stream<T> first, Stream<T> second)} that alternates elements from the streams first
 * and second, stopping when one of them runs of elements.
 */
public class Exercise8 {
    public static void main(String[] args) {

    }

    public static <T> Stream<T> zip(Stream<T> first, Stream<T> second){
       //TODO
        return null;
    }
}
